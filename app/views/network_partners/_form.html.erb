<%= form_for(@network_partner) do |f| %>
  <% if @network_partner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@network_partner.errors.count, "error") %> prohibited this network_partner from being saved:</h2>

      <ul>
      <% @network_partner.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :urn %><br>
    <%= f.text_field :urn %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
