<div class="form-horizontal">
  <%= form_for(@network_partner) do |f| %>
    <% if @network_partner.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@network_partner.errors.count, "error") %> prohibited this network_partner from being saved:</h2>

        <ul>
        <% @network_partner.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <%= f.label :name, class: "col-lg-2 control-label" %>
      <div class="col-lg-5"><%= f.text_field :name, class: "form-control" %></div>
    </div>
    <div class="form-group">
      <%= f.label :urn, class: "col-lg-2 control-label" %>
      <div class="col-lg-5"><%= f.text_field :urn, class: "form-control" %></div>
    </div>
    <div class="col-lg-offset-2"><%= f.submit class: "btn btn-primary" %></div>
  <% end %>
</div>